<%= simple_form_for(@restaurant) do |f| %>
    <div>
      <%= f.input :name %>
      <%= f.input :address %>
      <%= f.input :phone_number %>
      <div>
        <%= f.association :category, collection: Category.all %>
      </div>
        <!-- < f.input :category > -->
    </div>

    <div>
      <%= f.button :submit %>
    </div>
<% end %>



